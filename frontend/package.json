{
  "name": "adattoxte-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/build-seo.js && node scripts/check-links.mjs && node scripts/sync-vercel.mjs",
    "build": "vite build && cp dist/index.html dist/app.html && if [ \"$VERCEL\" = \"1\" ] && [ \"${PRERENDER_FAST:-1}\" != \"0\" ] && [ \"$VERCEL_ENV\" = \"production\" ]; then echo 'BLOCK: deploy cloud/hook in produzione disabilitato — usare solo il workflow prerender-full (PRERENDER_FAST=0). Se serve un preview, usare un branch non-main.'; exit 1; elif [ \"$VERCEL\" = \"1\" ] && [ \"${PRERENDER_FAST:-1}\" != \"0\" ]; then sh scripts/ensure-chrome-deps.sh && PRERENDER_FAST=1 node scripts/prerender.js; else sh scripts/ensure-chrome-deps.sh && node scripts/prerender.js; fi && node scripts/inline-css.cjs",
    "preview": "vite preview",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@sentry/react": "^10.72.0",
    "axios": "^1.7.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.24.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "puppeteer-core": "^25.10.0",
    "vite": "^5.3.1"
  }
}
