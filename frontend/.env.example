# Adatto x Te - Frontend configuration
# Copy to .env for local production build, or set in the hosting dashboard (Vercel).

# URL del backend in produzione (es. https://adattoxte-backend.onrender.com)
# In sviluppo lascia vuoto: il proxy di Vite usa http://localhost:3001
VITE_API_URL=
